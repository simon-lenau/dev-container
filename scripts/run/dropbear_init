#!/usr/bin/env bash

source "${DEV_CONTAINER_DIR}/dropbear_settings"

# Delete existing host keys
rm -rf ${DROPBEAR_FOLDER}/host_key_*

# To use predefined host keys, use this:
for host_key_type in "${host_key_types[@]}"; do
    cat "${dropbear_key_folder}/host_key_${host_key_type}.txt" | openssl base64 -d >"${DROPBEAR_FOLDER}/host_key_${host_key_type}"
done


cat "${SSH_KEY_FOLDER}/dropbear_rsa_host_key.txt" | openssl base64 -d >${DROPBEAR_FOLDER}/dropbear_rsa_host_key
cat "${SSH_KEY_FOLDER}/dropbear_ed25519_host_key.txt" | openssl base64 -d >${DROPBEAR_FOLDER}/dropbear_ed25519_host_key
cat "${SSH_KEY_FOLDER}/dropbear_ecdsa_host_key.txt" | openssl base64 -d >${DROPBEAR_FOLDER}/dropbear_ecdsa_host_key

authorized_keys_file=$HOME/.ssh/authorized_keys

if [ -f "${authorized_keys_file}" ]; then
    rm -f "${authorized_keys_file}"
else
    mkdir -p $(dirname "${authorized_keys_file}")
fi

cp "${SSH_KEY_FOLDER}/id_css-container.pub" >"${authorized_keys_file}"

chmod go-rwx "${authorized_keys_file}"

port=$(id -u)

if [ ! "${port}" -gt 0 ]; then
    port=11782
fi

# Add script to .bashrc that automatically changes the directory of vscode to $WORKDIR with a time limit
bashrc_path="$HOME/.bashrc"

begin_delim="# >>>>>>>>>>>>>>>>>>>> dev-container shell setup >>>>>>>>>>>>>>>>>>>> "
end_delim="# <<<<<<<<<<<<<<<<<<<< dev-container shell setup <<<<<<<<<<<<<<<<<<<< "

if ! grep -iq "${begin_delim}" "${bashrc_path}"; then
    cat >>${bashrc_path} <<EOF
${begin_delim}
export DEV_CONTAINER_DIR=${DEV_CONTAINER_DIR}
source \$DEV_CONTAINER_DIR/run/ssh_shell_setup
${end_delim}
EOF
fi

# Check if keys have been changed in comparison to those
# publicly available on git
${DEV_CONTAINER_DIR}/run/dropbear_check_keys || {
    # If tput is available: Store cursor position
    (command -v "tput" >/dev/null 2>&1) && tput sc
    printf "\n%s\n" "Do you wish to proceed? (yes / no)"
    read -n 255 -t 30 answer
    # If tput is available: Restore cursor position and clear everything afterwards
    (command -v "tput" >/dev/null 2>&1) && tput rc && echo -e "\033[0J" && tput rc

    if (echo "${answer}" | grep "[Yy]\([Ee][Ss]\)\{1,1\}"); then
        echo "OK"
    else
        exit 1
    fi
}

echo "----------------------------------------------------"
printf "Running on %s, port %s\n" "$HOST$HOSTNAME" "$port"
echo "----------------------------------------------------"

dropbear -F -E -s -p $port
