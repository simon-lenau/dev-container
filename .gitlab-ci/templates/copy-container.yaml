

.copy-container:
  image:
    name: quay.io/skopeo/stable:latest
    entrypoint: [""]
  stage: container-forward
  script:
    # Set Credentials
    - source ".gitlab-ci/set_credentials";
    - echo "BLABLA"
